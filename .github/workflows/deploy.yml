name: Auto-Deploy to Full Potential Server

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Deploy to Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /root/dashboard
            echo "📥 Pulling latest changes..."
            git pull
            echo "🛑 Stopping old container..."
            docker rm -f fpai-dashboard 2>/dev/null || true
            echo "🏗️  Building and starting new container..."
            docker-compose up -d --build
            echo "✅ Deployment complete!"
            echo ""
            echo "📊 Container status:"
            docker-compose ps

      - name: ✅ Verify Deployment
        run: |
          sleep 5
          echo "Testing homepage..."
          curl -s http://198.54.123.234/ | grep -i "unlock your full potential" && echo "✅ Homepage live!" || echo "⚠️ Check failed"
